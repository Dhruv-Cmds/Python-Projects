# JSON CLI User System

A small command line program written in Python to practice JSON file handling.

Features:
- Register users
- Login system
- Change username/password
- Delete users
- Persistent storage using JSON

This project was built as a learning exercise for JSON operations in Python.